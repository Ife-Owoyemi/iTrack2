<header class="navbar navbar-fixed-top navbar-inverse">
  <div class="navbar-inner">
    <div class="container">
      <%= link_to "iTrack", '/' , id: "logo", :class => 'brand' %>
      <nav>
        <% if signed_in? %>
          <%= render 'layouts/signedin' %>
        <% else %>
         <%= render 'layouts/notsignedin' %>
        <% end %>
      </nav>
    </div>
  </div>
</header>